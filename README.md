## Для деплоя в firebase (делаем локально)

`npm install -g firebase-tools`

`firebase login`

`npm run deploy-firebase`

## Для деплоя в aws (делаем с инстанса aws)

`npm i`

`npm run deploy deploy-aws`
